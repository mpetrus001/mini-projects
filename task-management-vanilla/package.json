{
  "name": "task-management-vanilla",
  "version": "1.0.0",
  "description": "A vanilla web front end for the Task-Management REST API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --watch db.json --port 5000"
  },
  "author": "Matthew P",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.16.2"
  }
}
